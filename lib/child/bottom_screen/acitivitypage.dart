import 'package:flutter/material.dart';
import 'package:vara/child/bottom_screen/acitivityhelper.dart';

class Activitypage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: ActivityHelper(), // Use ActivityHelper as the body of the page
    );
  }
}
